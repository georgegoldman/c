#include <stdio.h>
#include <stdlib.h>

int main()
{

    int age  = 40;
    double gpa = 3.7;
    char grade = 'a';
    char phrase[] = "what is going on?";

    printf("this is an integer => %d this is a double => %f this is a char grade => %c this is a string %s \n", age, gpa, grade, phrase);

    return 0;
}
