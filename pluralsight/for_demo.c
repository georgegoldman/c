#include <stdio.h>

/**
 * @brief 
 * main - entry point
 * Return: int i = 0
 * 
 */

int main(int argc, char const *argv[])
{
        /* code */
        for(int i = 0; i < 10; i++)
        {
                printf("Iteration #%d \n ", (i + 1));
        }

        return 0;
}
