#include <stdio.h>

/**
 * @brief 
 * main - entry point
 * Return: int = 0
 * 
 */

int main(int argc, char const *argv[])
{
        /* code */
        printf("Please enter an integer number: ");
        int n;
        scanf("%d", &n);
        if (n == 64)
        {
                printf("You entered 64. \n");
        }

        return (0);
}
